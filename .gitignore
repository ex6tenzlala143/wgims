*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Database backups and SQL dumps (may contain real production data)
/database/backups/
*.sql
*.sql.gz
*.sql.zip

# Test scripts (temporary testing files)
test_*.php
verify_*.php
backup_*.bat

# OpenCode local config and dependencies
.opencode/

# Local quarantine for AI-related / non-production files (not deployed)
# The folder stays on disk for review but is never committed or deployed.
/_AI_AND_UNRELATED_FILES/
.meta-panel-trigger
meta.bat
